# POOL.md — Standing Rules & Context for Pool Maintenance

**This file is the source of truth. Read it fully at the start of every session before doing anything.**
Owner: John — Guilford, CT. Inground vinyl-liner pool. Season 2026.

---

## How to work with me (Claude Code instructions)

1. At the start of each session, read this file and skim the last ~15 rows of `Pool_Log.xlsx` (Log tab) so you have recent history.
2. I run the test kit myself at noon and give you the numbers. You do NOT have a sensor — never invent readings.
3. Daily flow: I give you FC, CC, (sometimes a full panel), and any water-add times **at the noon test**. The **dose is added in the evening (~8–8:30 PM)** — I'll report the actual time after. **Pump runtime is automatic (7 AM–10 PM timer) — I don't report it; assume it.** **You pull the weather yourself** — I don't need to report that either. You:
   - Run `weather.py` to get current conditions for Guilford, CT and the matching `dose.py` weather bucket. Use that bucket and put the returned string in the Weather column. (If I describe the sky myself, prefer my observation; if `weather.py` fails — no network — fall back to asking me.)
   - Calculate the chlorine dose with `dose.py` (or the logic below).
   - Append the timestamped row(s) to the Log tab **in place** using `append_log.py` — edit the file, do not rebuild it. Preserve formatting and column order.
   - Tell me the dose and any flags (CC creeping, FC out of band, CYA recheck due).
4. Keep column order exactly: `Date, Time, Type, pH, FC, CC, FC loss (ppm/24h), Pred next-noon FC, Pred error (ppm), TA, CH, CYA, Chlorine added (gal), Cum. Cl (gal), Sun (hrs), Rain (in), Fill (gal), Water temp (°F), Water level (cm), Weather, Photo, Notes, Load` (23 columns). **Row 1 = header, row 2 = SEASON TOTALS (live formulas), data starts row 3.** Top two rows are frozen (`freeze_panes = A3`).
   - **Row 2 "SEASON TOTALS"** (merged A2:D2 label): live Excel `SUM()` formulas over the **Chlorine added (gal)**, **Rain (in)**, and **Fill (gal)** columns (range rows 3–5000, so it auto-includes every future row — no recompute needed). Always reflects the current season total at a glance without scrolling.
   - **FC loss (ppm/24h)** — on each **TEST** row: `prior TEST FC + (gal dosed since)×3.5 − this TEST FC`. The implied chlorine consumed since the last test. The key field for tuning the loss model and catching real demand early. (Blank/caveat across a reagent change — not comparable.)
   - **Pred next-noon FC** — on each **DOSE** row: the calibrated evening model's next-noon projection (`daily_calc.py`'s "Projected NEXT-NOON FC"). **Pred error (ppm)** — on the **following TEST** row: `this TEST's FC − the prior DOSE row's Pred next-noon FC` (signed: `+` = actual ran higher than predicted, i.e. loss was over-estimated). See "Prediction tracking" below.
   - **Cum. Cl (gal)** — on each **DOSE** row: running season total of liquid chlorine added. Tracks cost/usage.
   - **Sun (hrs)** — actual bright-sunshine hours for that date (`weather.py`), on the **TEST** row. The real driver of FC loss. Get values with `python weather.py --history N`.
   - **Rain (in)** — daily precip for that date (`weather.py`), on the **TEST** row. Watches CYA dilution.
   - **Fill (gal)** — on **EVENT - Fill** rows: gallons added via hose (minutes × ~3.1 GPM). Makes well-water top-offs summable data, not just prose in Notes.
   - **Water temp (°F)** — on the **TEST** row once the ladder/thermometer is in. Warm water raises chlorine demand. **Record the exact reading and keep one decimal (e.g., 80.5) — do NOT round. John values precise data; never round the values he reports.**
   - **Multiple tests in one day (e.g. a noon test + evening recheck):** log every test as its own TEST row — all readings are kept in the workbook, the Data-page log table, and `data.json`. But the **dashboard shows ONE canonical reading per day: the noon (first) test.** `index.html`'s chart JS dedupes to the first TEST per date (`allTests` → `tests`), so the chemistry trend, hero, gauge, and per-day derived stats use the noon value; same-day rechecks don't double-plot or double-count. Exception: the "Tests logged" season stat counts **all** actual tests (`allTests.length`), so a recheck still shows as work done. (John's call, 2026-07-02: rechecks are QC to catch mistakes, not trend data.)
   - **Water level (cm)** — added 2026-07-02 (col 19, between Water temp and Weather). Reading off a graduated ruler John mounted on the skimmer faceplate, on the **TEST** row. Tracks level over time (evaporation vs. fill/rain). Keep one decimal, never round. Baseline: **11.9 cm on 2026-07-02 (evening)**. NOTE: John flagged the ruler's adhesive "not sticking as well as led to believe" — watch for the ruler shifting (a sudden jump not explained by fill/evaporation likely means the ruler moved, not the water). Threaded into `data.json` but **no dashboard chart yet** — add one once there are ≥3 readings (like the pH/TA/CYA charts). It's the trailing new column on the Data-page log table for now.
   - **Load** — added 2026-07-19 (col 23, after Notes). Short tag for what the pool **experienced since the previous test** — i.e. the window whose FC drop *this* row measures — so it's filled in at noon from what happened yesterday afternoon/overnight. **Format (normalised 2026-08-13): `canonical[+canonical]; optional human qualifier`** — everything before the semicolon is the controlled class used for aggregation; free detail goes after it. **Controlled vocabulary:** `quiet`, `swimmers`, `party`, `dog` (live dog swimming), `storm`, `smoke`, `millipedes`, `caterpillars`, `leaves`, `earthworms`, `carcass` (dead animal). Combine classes with `+` (e.g. `millipedes+carcass`). Examples: `millipedes; heavy, same-day removal` · `carcass; mouse` · `quiet`. **Why:** the column had drifted into free text and was fragmenting into near-duplicate classes (`millipedes (heavy, removed same day)` vs `…same morning` vs `…LIGHT — fall clearly tapering`), splitting already-small groups; normalising merged those into a single `millipedes` class of 5. `analyze_loss.py` now splits on the semicolon and aggregates on the canonical part. Backup before the rewrite: `Pool_Log.backup-2026-08-13-preloadnorm.xlsx`. **This is the highest-value field for prediction:** the mass-balance re-analysis showed loss variance is driven by episodic organic load, not weather. `analyze_loss.py` prints mean loss per load class; first read (n=9) was **dog 6.89 / party 4.02 / storm 3.80 / quiet 3.07 ±0.61 / smoke 1.83**.
   - **Photo** — filename of the day's chlorine-check photo (see Daily photo below).
   - Record derived/weather fields on the daily **TEST** row; leave blank on DOSE/PUMP/etc. (cum_cl goes on DOSE rows, fill_gal on EVENT-Fill rows).
   - **After appending rows, run `python format_log.py`** — re-applies widths, centering (Weather & Notes left+wrapped), number formats, auto row heights, alternating-day gray shading, the totals-row styling, freeze panes, and the thick per-day box borders. Idempotent; presentation only, never touches data.
   - `append_log.py`'s style template is now the **last existing data row** (dynamic), not a hardcoded row number — stays correct through any future row/column insert.
5. Type tags: `TEST, DOSE, PUMP, WEATHER, NOTE, EVENT`. For `EVENT`, add a short qualifier so the Type cell says what it was, e.g. `EVENT - Open` (season open), `EVENT - Storm` (rain/TS), `EVENT - Backwash`, `EVENT - Fill`.
6. Be honest and math-grounded. If a reading looks off or contradicts a rule, say so. Don't reassure me — correct me.

---

## Pool specs

- Volume: **~31,400 gallons FULL** — measured hopper geometry 2026-07-17 (40×20 ft; 12 ft shallow shelf @ 3 ft; deep hopper 10×14 flat @ 8.5 ft; **all perimeter walls 3 ft**; avg depth ~5.25 ft — see `pool_volume.py`). Was ~34,400, a ~9% overestimate that assumed one 3→8.5 ramp (avg depth 5.75); the real bowl-shaped hopper holds less. **Current** volume tracks the level: **~30,700 gal at the 7/17 level** (shallow 34.5 in vs 36 in full = 1.5 in low).
- Pump: Hayward Super Pump SP1600, 3/4 HP, 120V, single-speed. **On an automatic timer: ON 7:00 AM, OFF 10:00 PM daily** (as of 2026-06-23). Assume this runtime by default — do NOT log a daily PUMP row. Only log a PUMP row when it **deviates** (outage, off-schedule manual run, timer change), and note the deviation.
- Filter: Pentair Nautilus FNS Plus 48 sq ft D.E.; multiport Praher SM10-2D.E.
- Water source: private well, ~3.1 GPM at the hose (slow — pace any fills).
- **Level / dilution math:** surface ≈ 800 sq ft (20×40), so **~500 gal changes the level ~1 inch.** Thus 30 min of hose ≈ 93 gal ≈ ~3/16"; ~500 gal of rain per inch falling on the pool. Hot-week evaporation ≈ ¼–½"/day (~125–250 gal). Use these for level-change answers and rain/fill CYA-dilution estimates.

## Chlorine / chemistry approach

- Chlorinate with **12.5% liquid sodium hypochlorite ONLY**. No trichlor/dichlor/stabilized products (CYA already too high).
- **Products on hand — verified from labels 2026-07-22 (photos in `reference/chlorine_labels/`, kept OUT of `photos/` so they never hit the website):** both are **Sodium Hypochlorite 12.50%, 1 US gal (3.785 L)** — confirming the `ppm_per_gallon` 3.84 and mass constant. **Current** case (1 gal left): **Austin's Pool Tec**, case code `26 174` = **made 2026 day 174 = Jun 23**. **Next** case (2 cases / 8 gal, FIFO after): **Champion Pool Shock (Liquid Chlorinator)**, code `26167` = **made 2026 day 167 = Jun 16**. Both **very fresh (<6 weeks)** and stored cool/dark → essentially full strength, so weak stock is an unlikely driver of the loss scatter. (Minor: the Champion was actually made 7 days *before* the Austin's, so strict FIFO is slightly inverted, but a 1-week gap on 5-week-old product is negligible — not worth reshuffling.) **The brand changes at the case boundary (Austin's → Champion), both 12.5% so expected identical** — which makes the dose-response test worth running on the **last Austin's gallon** and again on the **first Champion gallon** for a clean before/after.
- **Storage:** liquid chlorine kept in the **pool shed — ventilated, in the shade inside its box.** (Good — heat/sun/air degrade hypochlorite; these conditions slow it.) John also runs strict **FIFO** (oldest case used first) and the closet has **vents above and below the door** — correct on every count he controls: dark (UV is the fastest killer), ventilated (hypochlorite off-gasses; sealed storage builds pressure and traps heat), boxed, rotated.
- **⚠️ Chlorine STOCK STRENGTH is an unquantified error source (John's point, 2026-07-21).** 12.5% sodium hypochlorite decays continuously; the rate roughly **doubles per ~10–15°F**, so an outdoor closet in a CT July degrades stock far faster than a cool basement — and **age at the store before purchase is invisible to us and often the bigger factor.** Realistically expect **~5–15%** below label. **Why it matters here:** `analyze_loss.py` credits every dosed gallon with exactly `ppm_per_gallon`; if the real jug delivers less, the shortfall is silently booked as "loss" — so weak stock **corrupts dose-day mass balances only**, leaving no-dose days clean. A 10%-weak 1.5-gal dose hides ~0.6 ppm, which is **inside our ±1.2 ppm noise floor** — real, but currently undetectable. **Tested 2026-07-21 and NOT confirmed:** dose-day loss sd 1.93 vs no-dose 0.91 looked like proof, but excluding the two known outliers (dog day, 7/19 collapse) it inverted to 0.72 vs 0.91. Two outliers again — do not treat this as established.
- **To actually measure stock strength — controlled dose-response test (not yet run).** On an evening a **1.5–2 gal** dose is needed anyway (so it costs no extra chlorine): **(1)** test FC, **(2)** pour with the pump running, **(3)** wait **~60 min**, **(4)** re-test mid-pool, elbow-deep. `measured ppm/gal = ΔFC / gallons`, then add back ~0.13 ppm for the hour's decay. Compare to `ppm_per_gallon`: at full strength expect ~3.84; a materially lower number means degraded stock. **Use a big dose** — the ±0.2 ppm (one drop) resolution is ~5% of a 1-gal rise but only ~2.5% of a 2-gal rise. Evening only (no UV during the wait). Since pool volume is independently validated (geometry + back-test), a deviation points at **strength**, not volume. Re-running this on a **fresh case vs an old one** would directly quantify degradation. **Ask John to flag when he opens a new case** so step-changes in dose response can be spotted in the log.
- **Dosing rule: 1 gallon raises FC ~3.84 ppm** in this pool (~33 oz per +1 ppm). Rescaled 2026-07-17 from 3.5 when the volume was corrected to ~31,400 (the old 3.5 was tied to the 34,400 overestimate → doses ran ~9% weak on paper, part of the season-long high-prediction bias). `dose.py` updated. **The evening L24 model was fit against the old 3.5, but the 2026-07-17 back-test (`backtest_volume.py`) shows 3.84 + the current L24 is already near-unbiased — leave L24 as-is, just watch forward errors. fc_loss / prediction formulas now use 3.84.**
- **Round doses to the nearest half gallon, not quarter** (`dose.py` `dose_round = 0.5`). A quarter gallon is impractical to judge by eye in an opaque jug — half gallons (or a known full/leftover jug) are the practical unit.
- **CYA currently ~150 (high)** — fresh-reagent anchor 2026-06-24 (was ~160 on old reagents). FC band is keyed to CYA — see table below. When dosing, aim for the top of the band.
- **Don't fear the top of the band.** At CYA 150, FC anywhere in the band — and up into the high teens — is **safe and fully swimmable** (the high CYA buffers it; effective sanitizer tracks the FC/CYA ratio). So pre-loading to ~16–17 before a hot day or a party is fine. For reference, "shock" level at CYA 150 is ~**60 ppm** (impractical here) — a real reason high CYA is a liability, but not a day-to-day dosing concern.
- CYA being lowered **passively only** (backwash + fresh water, rain, top-offs). **NO active drain-down** (protect the low-yield well). Recheck CYA every few weeks via dilution test (50/50 pool + distilled, ×2).
- Don't dose pH/TA/CH routinely, but **don't blanket-ignore TA — monitor it via its effect on pH.** Measured TA ~170 (6/24) is **inflated by high CYA**: subtract ~⅓ of CYA (~50) → true carbonate alkalinity ≈ **120 (normal)**. So TA is not actually high right now. CH ~150 irrelevant on vinyl. **Act on TA only when pH repeatedly drifts above ~7.8–8.0 and won't hold** (high-TA upward bounce) — then lower TA with muriatic acid (+ aeration to bring pH back up). **Flag John when that pattern appears; tell him when it needs dealing with — don't act silently and don't ignore it.**
- Keep CC at/near 0. CC ≥ 0.5 that persists = flag (possible combined chloramines / not enough FC).

## FC target band by CYA (lower the band as CYA drops)

| CYA  | FC floor | FC target band | Aim when dosing |
|------|----------|----------------|-----------------|
| ~150 | 11       | 13–15          | ~15             |
| 100  | 7        | 7–11           | ~11             |
| 80   | 6        | 6–9            | ~9              |
| 50   | 4        | 4–6            | ~6              |

(Interpolate for in-between CYA. The band scales ~7–15% of CYA — floor ≈ CYA×0.07, target top ≈ CYA×0.10.)

## Weather → daily FC loss assumption

- Sunny / dry / high UV: ~4 ppm/day
- Partly cloudy: ~2.5–3 ppm/day
- Mostly cloudy / cool: ~1.5–2 ppm/day
- Rain / overcast all day: ~1 ppm/day (minimal UV)

Use the loss only to sanity-check whether tomorrow will still be in-band; the dose itself targets the top of the band now.

### Evening-dose loss model — CALIBRATED 2026-07-01 (use this for predictions)

From ~2 weeks of data. **Pre-6/24 (old-reagent) data excluded — unreliable.** Fresh-reagent evening-dose data (6/25–7/1) gives a clean model. **We dose in the evening, so predict next-noon FC as:**

> **next_noon_FC = today_noon_FC + dose_gal × 3.5 − L24**

where **L24** (full 24h loss, ppm) keys off the dose-day's full-day sunshine hours + water temp:

| Dose-day conditions | L24 |
|---|---|
| Rainy/overcast (sun < 3 h) | ~1.5 |
| Partly (sun 3–8 h) | ~3 |
| Sunny, cooler (sun ≥ 8 h, water < 80°F) | ~4 |
| **Hot & sunny (sun ≥ 8 h AND water ≥ 80°F)** | **~5** |

**Validated:** on the recent clean top-of-band days (6/27→28, 6/29→30, 6/30→01) predictions land within **±0.3 ppm**. Two known misses: **6/25→26** (heavy rain the *next* day, unforeseeable at noon) and **6/28→29** (dosed only 1 gal, so FC carried was low → less absolute loss). **Caveats:** loss scales with the FC level carried (dose to top of band for the model to hold); next-day rain lowers it; ±1 ppm on a 7-point set. All constants live in `dose.py` `CONFIG["evening_l24"]`; `daily_calc.py` prints the recommended evening dose + projection automatically. The old `dose.py` "projected next noon" line uses daytime `weather_loss` and is NOT the evening number — ignore it; use the EVENING-DOSE PLAN block.

**Weather is pulled automatically via `weather.py`** (Open-Meteo, free, no API key, stdlib only — location/tuning in its `CONFIG`). It returns a log-ready Weather string and the matching `dose.py` bucket (sunny/partly/cloudy/rain). **Bucket rule (v2, 2026-06-27):** meaningful precip → `rain`; otherwise by **realized sunshine fraction so far today** (hourly `sunshine_duration` ÷ daylight hours elapsed): ≥60% → sunny, ≥30% → partly, else cloudy. This replaced the old instantaneous-cloud snapshot, which mislabeled a sunny morning as "overcast" when a cloud happened to roll in at test time (6/27). Cloud cover is now only a fallback if hourly data is missing. Run `python weather.py` (human) or `python weather.py --json` (chaining).

### Prediction tracking (forward validation) — started 2026-07-01, moved to dedicated columns 2026-07-01

Log an explicit next-noon FC prediction **each evening** and score it the **next noon**, to watch whether the L24 model holds up *out-of-sample*. Tracked in two dedicated columns (not inline Notes text — that duplicated data and could drift out of sync):
- **Evening, on the DOSE row:** put `daily_calc.py`'s "Projected NEXT-NOON FC" value in **Pred next-noon FC**.
- **Noon, on the TEST row:** put `this TEST's FC − the prior DOSE row's Pred next-noon FC` in **Pred error (ppm)** (signed: `+` = actual ran higher than predicted, i.e. we over-estimated loss). Compute the error from the **rounded, displayed** prediction value (not extra hidden precision) so the two columns always subtract consistently if John checks by hand.
- **In-sample baseline** (model was fit on these — flatters it, so don't read too much into it): 6/27→28 pred 12.8 / actual 12.8 / err **0.0**; 6/28→29 pred 12.3 / actual 14.0 / err **+1.7** (miss: only 1 gal dosed → low FC carried → less absolute loss); 6/29→30 pred 14.2 / actual 14.6 / err **+0.4**; 6/30→01 pred 14.8 / actual 14.8 / err **0.0**. Clean-pair MAE ~0.1; ~0.5 with the miss. (6/25→26 and 6/26→27 excluded — next-day rain / the weather-mislabel false alarm.)
- **Forward test #1 = 7/1→7/2, predicted 15.1 (row 42).** Out-of-sample errors from here on are the real accuracy measure. Watch whether forward MAE holds ~±0.3; if one weather bucket shows a consistent bias, nudge that constant in `dose.py` `CONFIG["evening_l24"]` and note it here.
- **Logging mechanics (wired 2026-07-01):** `append_log.py` accepts `pred_fc` / `pred_error` (JSON keys) or `--pred_fc` / `--pred_error` (CLI).
- **Signed-bias watch → RESOLVED + retuned 2026-07-04 (7 scored):** errors — 0.0, +1.7, +0.4, 0.0, +2.1 (7/2), +0.9 (7/3), **+1.7 (7/4)** — were **all ≥ 0**: actual FC always landed **at or above** the prediction → the L24 model **consistently over-estimated loss**. The three hot/sunny days ran +2.1, +0.9, +1.7 (mean +1.6); actual hot-day noon-to-noon losses were ~2.9 / 4.1 / 3.3 (avg **~3.4**), not the modeled **5.0**. 7/4 was the clean confirmation — FC landed high (16.8) *despite* extra CC-clearing demand that should have pulled it down. **Action taken: lowered `dose.py` `CONFIG["evening_l24"]["l24_hot_sunny"]` 5.0 → 4.0** (small conservative cushion above the ~3.4 mean; can go to ~3.5 if it keeps over-predicting). Note this collapses hot_sunny to equal `l24_sunny` (4.0) — the cooler-water sunny bucket is **unvalidated by recent data** (every recent sunny day had hot water ≥83°F), so leave it until a cool-water sunny day appears. **Watch after the 7/6–7/7 rain / mid-week when hot weather returns:** confirm forward errors now center on ~0 rather than staying positive. Dashboard visualizes this: "Prediction error by day" bar + "Prediction error vs. sun" scatter (added 2026-07-03).
- **Loss-model calibration → buckets NARROWED 2026-07-06 (John's insight):** John noticed the predictions ran "backwards" by weather — over-predicting loss on sun, under-predicting on rain (7/6 was the first negative error, -1.3, on a storm day). Verified against all fresh-reagent daily losses: **actual loss barely tracks sun** (least-squares slope **~0.08 ppm/sun-hr** vs the ~0.25 the buckets implied — 3× too steep), and same-weather scatter is huge (**"sunny" days alone ranged 1.2–5.1 ppm**). Root cause: at **CYA ~150 the stabilizer shields UV**, so sunny ≈ cloudy for FC loss; the real driver is a near-constant **~3 ppm/day background demand ± large noise (stdev ~1.3)**. Fix: compressed the buckets toward ~3 — **`l24_rain` 1.5→2.5, `l24_sunny`/`l24_hot_sunny` 4.0→3.5** (partly stays 3.0). Spread went from 2.5 → ~1.0. **STANDING TASK (John's request): keep analyzing loss-vs-weather AND loss-vs-reagent-era as data accrues** — pre-6/24 old-reagent losses stay EXCLUDED as unreliable; re-run the loss-vs-sun regression every week or two on the growing fresh-reagent set; bump `l24_hot_sunny` back toward ~3.8 if hot days keep running higher than 3.5; watch whether the narrowed buckets actually center the forward errors near 0.
  - **Noon (fully automatic):** `daily_calc.py` now auto-finds the last DOSE row's `Pred next-noon FC` and prints a `PREDICTION CHECK` block with the error already computed — just copy that number into `rows.json`'s `pred_error`. No manual subtraction.
  - **Evening (still needs judgment, but no manual arithmetic):** `daily_calc.py`'s "Projected NEXT-NOON FC" uses the model's own auto-recommended dose — if the final pour differs (buffer decisions, half-gallon jug rounding, etc.), that number is wrong for what's actually logged. Recompute it for the real pour with `python dose.py --evening --fc F --sun S --water_temp T --dose FINAL_GAL`, then copy its "Projected NEXT-NOON FC" into `rows.json`'s `pred_fc`. (Confirmed this matters historically: 6/27's own model default was 2.0 gal, but 1.5 was actually poured — using the default would've logged the wrong prediction.)
  - `daily_calc.py` stays read-only by design (per its docstring) — it never writes to the workbook itself; append_log.py remains the sole writer, after Claude's judgment step.

## Environment (this machine)

- Windows box. **Use `python` (NOT `python3`).** `python3` hits the Microsoft Store stub and fails; `python` is on PATH → `C:\Python314\python.exe` (Python 3.14.3, `openpyxl` 3.1.5 installed). Run scripts as `python dose.py ...` and `python append_log.py ...` (the scripts' `#!/usr/bin/env python3` shebang is harmless and ignored here).
- **Permissions:** the app's "Allow bypass permissions mode" toggle is ON, and it cuts prompts down a lot, but **not to zero — an occasional command still prompts** (confirmed 2026-07-01, `format_log.py` prompted after several silent runs). Cause unconfirmed; don't burn time re-testing it. `.claude/settings.json`/`settings.local.json` allow-rules and `defaultMode` were tried first and did NOT reliably work — don't retry those. If a prompt appears, just click Always Allow and move on; it's not worth chasing further.
- **Editing the Word test guide (`.docx`):** use the docx skill's flow — `unpack.py` → edit `word/document.xml` → `pack.py --original …` — but run its scripts with **`PYTHONUTF8=1`**, or they crash on this box's cp1252 default (the validator reads XML / prints a `→`). Needs `defusedxml` + `lxml` (both pip-installed 2026-07-01).

## Daily calc tool (`daily_calc.py`)

To minimize back-and-forth, the noon routine is consolidated into one script:
1. Write today's readings to `today_input.json` (next to the scripts): `{"date", "time", "fc", "cc", "water_temp", "weather_override"?, "rain_in_override"?}`. Use `weather_override` when the auto weather bucket clearly misjudges the day (e.g. a big daily-precip total from overnight rain makes a currently-clear/sunny midday read as "rain").
2. Run `python daily_calc.py` (no args) — prints the weather read, sun/rain for the day, the implied FC loss vs. the last TEST (same formula as the FC-loss column), and the `dose.py` recommendation, all in one shot.
3. Decide the final dose/notes (same judgment as always — e.g. hold buffer on hot/sunny days even if the model says 0), write `rows.json`, then `python append_log.py --json rows.json` and `python format_log.py` as usual.

## Testing method (keep consistent)

- **Full step-by-step for all six tests** (pH, FC, CC, TA, CH, CYA) + tube selection and the Watergram balancing step: [`Taylor_K2006_Testing_Guide.docx`](Taylor_K2006_Testing_Guide.docx) in the project folder. Its reference constants were reconciled to this file on 2026-07-01 (34,400 gal, 3.5 ppm/gal, ~36 oz/ppm).
- FC/CC: **ALWAYS 25 mL fill, drops × 0.2.**
- **✅ JOHN ALWAYS REPORTS A DOSE (his rule, 2026-08-25). No report = it did not happen.** He will always tell Claude when he adds chlorine, so **silence means he forgot to DOSE, not that he forgot to report.** Treat an unreported interval as undosed, full stop — no need to ask. **Corollary — flag it proactively:** if a dose was recommended and no report follows, say so at the next test rather than silently absorbing it, especially when FC is drifting toward the floor.
- **🔴 NEVER credit a dose in the mass balance without a logged DOSE row (2026-08-25).** A **recommended** dose is not an **administered** dose. On 8/25 Claude folded a 1.0 gal dose into the loss calculation because it had been *recommended* on 8/24 — John never poured it, and the last real dose was 8/23. That inflated the computed loss from **1.41 to 5.34 ppm/24h**, turning an ordinary day into a reported "second highest of the season". Worse, Claude had predicted that morning that a dead mouse would drive an elevated reading, then used the self-inflated figure to declare the prediction confirmed — **the evidence came from the error itself.** John caught it by asking whether he had actually dosed. **Always check for the DOSE row; if there is none, treat the interval as undosed and say so.**
- **🔁 PAIRED READING ON EVERY REAGENT CONTAINER SWITCH — standing practice.** Run the FC test on the OLD container and immediately again on the NEW one, same spot and technique. Agreement within a drop or two = no seam, series continues; a meaningful gap = trust the FRESH container and mark a deliberate step change (as on 8/20, when a near-empty jar was found reading ~2 ppm LOW). Reagent transitions have corrupted this record **twice** — the July FAS titrant over-reading ~1.2×, and the 8/20 DPD case — so this check is not optional. **Next switch pending:** John received a fresh Taylor **R-0870, 10 g** jar on 2026-08-23 and will pair it against the current (good) container when that one runs out. **Note the size:** 10 g is ~20 tests at the nominal 0.5 g dipper, and John deliberately heaps it, so realistically **10–15 tests ≈ two weeks of daily testing**. That short life is also why the previous jar degraded — a small container opened daily accumulates many air cycles before it is used up. Reorder early; keep the silica gel pack in with the powder.
- **🔴 DPD POWDER MUST BE IN VISIBLE EXCESS — standard adopted 2026-07-28.** Add enough R-0870 that, after stirring, **undissolved powder remains on the bottom of the vial.** That is an *objective, verifiable* criterion (you can see it) rather than a subjective "generous scoop", so it stays consistent day to day. **Why it matters:** DPD converts free chlorine into the pink dye that FAS then titrates — if there is not enough powder to convert ALL the chlorine, the uncounted portion reads as **LOW, never high**. This binds hardest at the **FC 13–18 this pool runs**: the more chlorine, the more powder needed, so a light scoop matters far more at FC 15 than at FC 3. **Evidence (7/28):** two back-to-back samples, same spot, same technique, minutes apart — John's normal (already generous) scoop gave **36 drops**; adding "way more than needed", the ONLY variable changed, gave **46 drops**. More powder → higher reading on the same water is exactly what DPD limitation predicts. **Diagnostic rule that follows: when replicates disagree, the HIGHER reading is the more likely truth — BUT CHECK THE MASS BALANCE FIRST (refined 2026-08-07).** That heuristic assumes DPD limitation is the error mode; a simple **miscount reads high** just as easily. On 8/07 the higher of two readings (16.8 vs 15.4) implied chlorine *increasing* with no dose — physically impossible — so the lower was correct. **Physics beats the heuristic: only fall back on "trust the higher" when both candidates are physically admissible.** **And as of 2026-08-07 the heuristic is effectively DEAD for this pool:** John confirms he now reliably over-adds DPD (visible excess, his standard since 7/28), which **closes the "reads low" mechanism entirely** — leaving **miscounting, which reads HIGH, as the main remaining error mode.** So when replicates disagree now, the **higher** reading is the more suspect one — the reverse of the 7/28 rule, which was written while powder quantity was still the open question. **Open hypothesis (NOT acted on):** FC may have read low at times this season, more so on high-FC days, which would inject one-directional noise into the loss series — do not retune anything on it without direct evidence. Practical note: swirl thoroughly, and read the endpoint against the **liquid** colour, ignoring settled powder (let it settle a few seconds if it obscures the view).
- **🔢 John's miscount mode is a WHOLE TEN, not a drop or two (his own observation, 2026-08-07).** When he loses count it is essentially always by **10 drops = 2.0 ppm** — being at 70 while counting 60, etc. — not by 2–3 drops. **Two consequences:** (1) **GOOD NEWS for detection** — a 2.0 ppm error is large enough that it usually breaks the mass balance outright or produces a visibly anomalous loss, unlike the small errors that slip through undetected; (2) **new trigger: any reading landing ~2 ppm off what the trend predicts should be treated as a possible miscount and RE-TESTED immediately**, before reaching for a chemistry explanation. This fits 8/07: a first count of 84 was likely a true ~74 read as 84, which sits just 3 drops (ordinary scatter) from the careful re-count of 77.
  - **⚠️ The slow-drop protocol is itself part of the cause (John, 2026-08-07).** He does not want counting aids and does not need them — the volume of tests this season speaks for itself. His point: **the deliberate slow drops Claude introduced on 7/22 roughly doubled the time on task**, and a 75-drop titration that drags is precisely where attention slips. That trade-off was Claude's doing and was not weighed at the time.
  - **Two real fixes (not crutches), both untested so far:**
    1. **Consistency of drop SIZE is the actual requirement — not slowness.** Bulk-adding was wrong because fast-squeezed drops are physically *smaller* than gravity-formed ones. A steady moderate rhythm with uniform pressure should preserve drop size. The 7/22 experiment compared *bulk-dump* vs *very slow* — **moderate-steady was never tested**. Run a paired test (moderate pace, then immediately slow) — if they agree within a drop or two, use the faster tempo permanently.
    2. **10 mL sample instead of 25 mL** → each drop = **0.5 ppm**, so FC 15 is ~**31 drops instead of 77**: less than half the time and far less to hold in mind, and it stretches the reagent. Trade-off is coarser resolution (0.5 vs 0.2 ppm), but **predictable quantization beats an unpredictable 2.0 ppm miscount**. If adopted, run one day at BOTH volumes to confirm agreement before trusting the new baseline.
- **🔴 NEVER bulk-add titrant — measured 2026-07-22, worth ~0.9 ppm.** John used to tip in ~50 drops at once (knowing the endpoint was far off), then finish dropwise. Paired same-spot tests 55 min apart: **88 drops (bulk-add) vs 83 (slow)** — decay explains only 0.6 drops, so bulk-adding reads **+4.4 drops = +0.87 ppm HIGH**. Mechanism is mechanical, not chemical: rapidly squeezed drops are **smaller** than slow gravity-formed ones, so more are needed to reach the endpoint. (The chemistry itself is indifferent — FAS-DPD stoichiometry doesn't care how the titrant arrives.) **This single artefact resolved the 7/22 "impossible" reading** (implied loss −0.42 → +0.59 ppm). **Because it was used *sometimes*, it injected ~0.9 ppm of VARIABLE bias, which propagates to ~±1.2 ppm on every computed daily loss — comparable to the entire observed loss scatter (sd 1.57).** Much of the "unpredictable demand" chased 7/17–7/22 may simply be this. **STANDARD PROCEDURE: every drop slow and deliberate, bottle held vertical, consistent squeeze, swirling throughout. No shortcuts even when 40 drops from the end.** *Falsifiable prediction (set 7/22): loss scatter should fall from ~1.57 toward ~1.0 or below within 1–2 weeks. If it doesn't, the demand variance is real and the measurement is exonerated.*
- **Cross-pool gradient ~0.8 ppm — sample the SAME spot every time (2026-07-22).** Both slow-technique, ~15 min apart: **red spot 83 drops (16.6) vs green spot, opposite long side, 79 drops (15.8)**. This does *not* contradict the 7/19 split test (58/57) — that compared two spots on the **same side**, so it couldn't detect a cross-pool gradient. Since John always samples **red** (long side, ladder side, mid-pool, elbow-deep), it's a **constant offset that cancels out of day-to-day loss math** — consistency of location matters far more than which spot is "correct". **Keep using red; if the spot ever changes, note it, because the step will look like a chemistry event.**
- **⚠️ The 15.81 cm ruler mark is a REFERENCE, not "full" (corrected by John, 2026-07-22).** It is simply where the shallow end reads 36 in; **there are still several inches of freeboard above it** before the pool would actually overflow. At 17.0 cm the water was at the **top of the skimmer box** and still not near overflowing. Consequences: (a) Claude's "it will overflow ~800 gal" predictions were built on a wrong assumption — **the pool has not overflowed at any point**, so no chlorine has been lost that way; (b) the `FULL_GAL`/`FULL_LEVEL_CM` pair in the scripts is still arithmetically correct (at ruler 15.81 the volume is ~31,400 gal, and 196.3 gal/cm holds because the walls are vertical) — only the **name** is misleading. (c) **John's tape is MAXED OUT at 17.0 cm** — above that the level cannot be read at all, so a longer strip is needed before the next big rain if we want to keep the water-balance math.
- **Rain: the feed is an estimate, the ruler is ground truth — and runoff is NOT the explanation (John, 2026-07-22).** Claude hypothesised deck runoff to explain the pool gaining 3–4× the grid rainfall; **John says little water runs into the pool, so that is retracted.** The real cause is **local rainfall variability**: his town sees very different totals on the southern coast vs. the north vs. the east (cf. the 7/18 storm — Guilford 0.80 in, Northford 3.72 in, Salem 4.68 in). Unless a gauge sits on his property, the feed's precip figure should be treated as a rough regional estimate only. **Always derive water-in from the level delta (× ~196 gal/cm), never from the precip column.**
- **Standing practice — a surprising reading gets an IMMEDIATE second sample (2026-07-22).** Claude talked John out of re-testing an anomalous FC by citing the 7/19 split test (58/57 drops) as proof his readings reproduce — but that was days earlier on a different sample and says nothing about whether *that day's* reading was repeatable. **A replicate is only informative in the moment**; hours later, decay has moved the target. So: anomalous number → draw a second sample right away, before anything else.
- **Actual pool usage — John, 2026-07-20 (essential context).** Use has been **minimal** this season; **no heavy swimming load**. The dog is in **2 minutes at most** (a lap or two), so the 7/10 `dog` tag must **not** be read as a large bather load. **John will explicitly flag any real swimming load.** Consequence: Claude's "episodic bather load" story for the loss variance is **not supported** — with use this low, the residual demand is far more likely **environmental organic input** (this pool is ringed by trees: pollen, leaves, insects, plus storm wash-in), which we neither measure nor can forecast.
- **2026-07-20, n=10 — every candidate predictor got WEAKER with more data**, the hallmark of noise rather than signal: sun r +0.49 → **+0.15**; FC-carried r −0.73 → **−0.44**; water temp −0.00 → **+0.11**. Flat mean and the old sun-buckets tie at **MAE 1.17**. **Conclusion: nothing we currently measure predicts daily FC loss; the practical floor is ~±1.2 ppm.** **STOP building loss models on <20 points** — three attempts (sun buckets, first-order FC, load classes) have each been broken by the next reading. **Operating policy instead of prediction:** keep FC in the mid-upper band, test daily, dose reactively as it drifts toward the lower band; do not chase precision the data cannot support.
- **✅ SETTLED 2026-08-12 — the predictors COMBINED are worse than a flat mean (`analyze_multivar.py`).** John asked the right follow-up: individually weak predictors can sometimes combine usefully. Tested by **leave-one-out cross-validation on 28 day-pairs** (refit 28×, each time scoring only the held-out day). Result — flat-mean baseline **MAE 1.29**; every single predictor is worse (sun 1.32, temp 1.30, FC carried 1.35, rain 1.31, gal dosed 1.34); and combining makes it **monotonically worse**: 2 predictors 1.35, 3 → 1.42, 4 → 1.43, **5 → 1.51**. That steady degradation as predictors are added is the signature of fitting noise. **The same models scored IN-SAMPLE show the opposite** — flat 1.25, 3 predictors 1.22, 5 predictors 1.20 — i.e. more variables always looks better on data the model has already seen. That illusion is mathematically guaranteed and is exactly the trap this test exists to avoid. **CONCLUSION: use the flat mean. Do not build, combine, or revisit a multivariate FC-loss model without a genuinely new KIND of measurement** (not more of the same variables) — and even then, validate out-of-sample before believing it.
- **✅ CLOSED 2026-08-24 — the reagent-drift hypothesis is dead.** After the 8/20 discovery that a near-empty DPD container read ~2 ppm low, it was flagged that a *growing* offset (unlike a constant one) would have INFLATED the elevated mid-August losses. A falsification criterion was set in advance: *a single 3–5 ppm day on clean powder kills it.* Clean-powder pairs came in 0.52 / 1.15 / 1.99 / **3.64**, and the 3.64 exceeds three of the five degraded-era values, so the distributions overlap and the borderline rank result vanished. **The mid-August losses were real demand.** The apparent early separation was confounding — the clean-powder window coincided with the end of the insect wave. Ninth hypothesis raised and rejected on this problem; the criterion being written down *before* the data arrived is what made it decisive rather than arguable.
- **WATCH (not a finding) — possible low-demand regime.** Early-July windows (7/03–7/09, holiday week + the 7/5 party) averaged **3.75 ppm/day**; the recent minimal-use quiet days ran **1.83 and 0.59** (storm day 3.80 excluded). If several consecutive no-dose days keep landing ~1 ppm, the current baseline is far below the modelled 3.5 — meaning doses last much longer and we are over-dosing. **n=2; do NOT change `l24` on this.**
- **John's FC titration technique — CONFIRMED 2026-07-20. Do NOT re-litigate it.** He adds drops while **swirling continuously**, stops at the **first clear**, and **never waits-and-re-adds**. **The pink does not return in this pool/kit** — the classic high-CYA "endpoint fade" simply does not manifest here. Readings also reproduce across back-to-back samples (58/57 drops, 7/19). **Standing rule: when a reading is surprising, the explanation is NOT the endpoint or the operator — look at load/demand first.** Claude raised endpoint-fade twice (7/17 and 7/20) and was wrong both times; it is an unfalsifiable excuse that hides a model that doesn't fit.
- **pH: chlorine interference TESTED and RULED OUT (2026-08-02) — do not re-raise.** Phenol red is oxidised by chlorine above ~10 ppm in general, so at this pool's routine FC 13–18 a high-biased pH was suspected. John re-ran the test **with 1 drop of R-0007 (thiosulfate) swirled in before the phenol red, at FC 15.8, and got the same result (~7.7–7.8)**. So there is no meaningful interference here and **the R-0007 step is not needed routinely** — season pH readings can be taken at face value. Note the comparator has no 7.7 patch (standards 7.0/7.2/7.4/7.6/7.8/8.0), so 7.7 is always an interpolation; that ambiguity is inherent, not an error.
- **pH CADENCE — cut back to periodic 2026-08-14 (John's call).** pH was run daily through early August as the watch signal for the low carbonate TA (~71). It came back **flat for two solid weeks** (7.7/7.8 alternating, 8/03–8/14, no drift either way — and since the comparator has no 7.7 patch that wobble is interpolation, not movement). pH is a slow-moving parameter, so **weekly is ample to catch a real drift** long before it matters. **New cadence: test pH with the periodic CYA panel**, plus **TA at the same time** (TA is the underlying variable; pH is only the symptom), plus **after any significant rain stretch** — rain is the one thing that actually moves this pool's pH, being mildly acidic and diluting the alkalinity that buffers it. **Triggers to resume DAILY pH:** any reading outside **7.6–8.0**, or carbonate TA below **~60** on the next panel. Saves R-0004 as a side benefit.
- **⚠️ TA MECHANISM CORRECTED 2026-08-14 — the July drop was NOT rain dilution.** On 8/01 Claude attributed TA 170 → 120 to a wet July diluting alkalinity. That contradicts the CYA reasoning recorded the same day (rain dilutes only temporarily; evaporation removes pure water and re-concentrates, so dissolved mass leaves only when WATER leaves). Both cannot hold. **John has not backwashed in weeks**, so water loss is not the answer either. The likely real mechanism is **acid from chlorine consumption**: every ppm of FC consumed releases H⁺, which eats carbonate alkalinity — a genuine, cumulative, ONE-WAY loss driven by chlorine use (43.5 gal this season), unlike rain. **Consequence: expect TA to keep declining as the season runs**, since the driver is ongoing dosing rather than weather. Check it on each periodic panel rather than assuming it settles.
- **If TA needs raising: sodium BICARBONATE (plain baking soda).** Sold as **"Alkalinity Increaser"** — do **NOT** buy **"pH Increaser"**, which is sodium *carbonate* (soda ash), a different chemical that raises pH hard and TA little. Pool-branded product is baking soda at a markup; a 13.5 lb warehouse-club bag is identical and stores indefinitely dry. **Dose for this pool: ~4.7 lb per +10 ppm TA** (carbonate 71 → 90 ≈ **9 lb**), so one bag covers a full correction with margin. Broadcast with the pump running (or pre-dissolve in a bucket), add in portions, retest after ~24 h of circulation. Expect pH to rise slightly (7.7–7.8 → ~7.9–8.0), which is fine. **Number to watch:** the carbonate-TA-below-60 trigger translates to a **RAW TA reading below ~110 (≈11 drops)** at the current CYA ~148 — and 8/01 measured 12 drops, i.e. one drop from the watch zone.
- **pH/TA are coupled — matters now that carbonate TA is ~71.** Muriatic acid lowers **both** pH and TA, so the reflex "pH is high → add acid" would push already-low alkalinity down further. If pH climbs above ~8.0: small acid dose **plus baking soda** to hold TA. Aeration raises pH **without** changing TA (useful if TA ever needs raising while pH is already fine).
- **Sample location — tested 2026-07-19, does NOT materially matter.** Paired samples minutes apart: deep end beside the ladder = 58 drops (11.6), mid-pool same side = 57 drops (11.4). One drop apart = the test's own resolution, so **the pool mixes well** (pump 7 AM–10 PM) and past readings were not location-skewed. Still good practice: **elbow-deep (~18 in), pump running, away from returns/skimmer/steps**; don't skim the surface, especially right after heavy rain.
- Routine (adopted 2026-06-25): **test at noon, dose in the EVENING.** Test FIRST at noon (I size the dose off that reading) → John adds the chlorine in the evening with the pump still running. **This pool is out of direct sun by ~6:30 PM** (shaded earlier than the ~8:25 sunset), so the dosing window is **~6:30–9:15 PM** — any time in there works. **Hard rule: pour by ~9:15 PM at the latest** so it mixes before the 10 PM pump-off — **never pour at/after pump-off** (unmixed liquid chlorine can bleach the vinyl liner). Why evening: skips peak-UV loss (less chlorine used), puts FC highest overnight when algae risk peaks, and makes next-noon projection more predictable.

---

## Daily photo (chlorine-check picture)

- John saves the day's photo(s) in the **evening (~6:30 PM, when he doses)** — consistent comparison shots — to **`C:\dev\pool\photos\`**, named **`YYYY-MM-DD.jpg`** (match the test date). **So at the noon test the file usually isn't there yet — analyze it that evening or next session; do NOT flag it "missing" at noon.**
- Record the filename in the **Photo** column of that day's TEST row.
- **Each day, open and analyze the photo** (Read the image): assess water clarity/cloudiness (early-algae warning before it's obvious), color cast, and **metal-stain progression** on the shallow-end steps/floor (ties to the staining open item). Note anything notable in the TEST row's Notes; flag real changes to John.
- **Videos (added 2026-07-05):** John may drop a phone video (`.MOV`, ~40MB) into `photos/`. The site renders a `<video>` player on the Photos page, **above** that day's photos, under the date heading. Workflow: **transcode the `.MOV` to a small web `.mp4` with ffmpeg, named `YYYY-MM-DD_vN.mp4`** (the date prefix is how `build_site.py` buckets it), then commit the `.mp4`. The large `.MOV` originals are **gitignored** (`*.MOV`) — only the compressed `.mp4` goes in the repo. ffmpeg is installed (winget `Gyan.FFmpeg`; resolves in a fresh shell, or use the full path under `%LOCALAPPDATA%\Microsoft\WinGet\Packages\Gyan.FFmpeg_*\...\bin\`). Transcode recipe that got a 22s 1080p clip from 41MB → ~8MB (pool water shimmer compresses poorly, so it needs aggressive settings): `ffmpeg -i IN.MOV -vf "scale=960:-2,fps=24" -c:v libx264 -crf 30 -preset slow -movflags +faststart -c:a aac -b:a 64k -pix_fmt yuv420p -y OUT.mp4`. ffmpeg auto-applies the phone's rotation flag, so portrait clips come out upright (the `.day-video` CSS caps height at 70vh so they don't tower).

## Equipment routine

- **Robotic vacuum — DAILY, morning run with a NOON canister pull (routine settled 2026-08-10).** Daily vacuuming is a big part of why this water stays clear. **Current cycle:** *~7:30 AM* vacuum in → runs until the battery dies, **finishing ~10:30–11 AM** (it does not run all day) → *~12:30 PM* one trip: **FC test + pull the canister + photograph it + empty** → charge through the afternoon → *evening* dose only → ready for 7:30 AM.
  - **Why this configuration wins.** Caterpillars/millipedes arrive **overnight** (8/09: pool clean at vacuum-finish, fully loaded by noon), and the 8/05–8/07 data shows **residence time, not quantity, drives chlorine cost**. Comparing the three schedules tried: *old (noon vacuum, evening pull)* — arrivals sat 6–12 h, canister soaked 4–5 h; *morning vacuum, evening pull* — arrivals 1.5–7.5 h but canister soaked **9 h**, giving back most of the gain since a canister of decomposing matter keeps leaching through the mesh; ***morning vacuum, noon pull* — arrivals 1.5–7.5 h AND canister only ~1.5–2 h.** Best on both counts, with no extra trip.
  - **Two side effects to remember:** (1) the FC test now happens *after* a vacuum run rather than before — fine for consistency since the pool is well mixed (7/19 split test), but it is a change in conditions, so do not mistake any step in the readings for chemistry; (2) the **debris photo is now taken at a consistent time on a fixed window** (7:30 AM start → ~12:30 PM pull), which makes the `debris/` series far more comparable day-to-day than the drifting evening pulls were. **Entries before 2026-08-10 cover different, longer windows — do not compare volumes across that seam.**
- **⚠️ After a visible load event, pull the canister the SAME day.** The mesh contains solids but water flows through it, so a canister full of decomposing organic matter left in the pool overnight keeps leaching dissolved organics and ammonia — the load is **concentrated, not removed**, and continues to consume chlorine. Negligible for a few leaves on an ordinary day; material after a millipede fall, storm debris, or anything visibly accumulating (cf. 2026-08-05, loss 4.90 ppm). **Interpretation note:** when a big-loss day follows a heavy-debris day, part of the demand may be debris *retained in the canister* rather than newly arrived.

## Open items (carry across sessions — update as they resolve)

- **➡️ CURRENT STATE & ACTIVE ISSUES → see `HANDOFF.md`** (written 2026-07-16): the bad-FC-reagent saga (FC over-reads ~1.5×, logged BLANK, tracked by drop-count trend; **TF-100 arriving Fri 7/17 → recalibrate**), the **manual gh-pages deploy** workaround (GitHub Actions incident since 7/9 — check if it's cleared), the two-night concert pages, and the live chemistry snapshot. Read it alongside this file at the start of any new session.

- **July 5 party (1 PM):** peak sun + heat + bather load. Plan: **Sat 7/4 evening pre-load FC to the top of band (~16–17)** for cushion; **Sun 7/5 test before 1 PM**, small morning top-up if FC slipped (FC ~16 at CYA 150 is swimmable); pump already on 7 AM–10 PM covers the party; **Sun evening test + dose back up** (expect a big bather-load drop); **Mon 7/6 watch CC** (sweat/sunscreen → chloramines; if CC ≥0.5 and holds, push FC higher to burn it off). Stocking extra liquid chlorine (~12–15 gal) for the stretch.
- **Metal staining (shallow end near returns):** suspected iron/manganese from well water. Confirm with vitamin C / ascorbic acid spot test (In The Swim A+ 2 lb pure ascorbic acid, ordered). Treatment deferred to fall closing (requires dropping FC to ~0). **Photo close-ups 6/27–6/29 show no clearly visible iron/rust stain — normal speckled liner; staining unconfirmed/not visibly progressing. Spot test is the definitive check.**
- **Sequestrant (prevention):** **Pool Mate Metal Out** (phosphonic acid = good chlorine-stable type). **Status: John buying a 1-qt bottle (with the chlorine run) — on hand but HOLDING, no dose yet (his call).** Dose when the cumulative well water (Fill column total) is meaningful or he decides — e.g. a **~16–24 oz maintenance dose covers the whole hot-week fill stretch** (per-fill metal load is tiny). Label maintenance = 1 qt per 20,000 gal monthly; for this pool ~16–24 oz to establish a residual, then ~1 qt/month or after a big fill. Add with pump running, let circulate; high FC depletes it (hence monthly). Does NOT remove existing stain — that's the fall ascorbic-acid job (the label's "colored water" procedure = same idea: FC→0, pH→6.8).
- **Reagent refill:** ✅ done — fresh FAS-DPD reagents in use as of 2026-06-24. (Old reagents read CYA high ~160 and gave phantom CC ~0.4.)
- **CYA recheck:** ✅ done 2026-06-24 — fresh-reagent anchor **CYA ~150** (down from old-reagent ~160). `cya_current` updated to 150 in `dose.py`; band lowered to floor 11 / aim 15. The 160→150 is **both** fresh-reagent re-anchoring **and** ~1–5 ppm of real rain dilution (6/22–6/23, ~250–1,000 gal into 34,400) — can't cleanly separate, but passive lowering is contributing. Recheck again in a few weeks; note rain/top-offs that nudge it down.
- **CC resolved:** fresh reagents read **CC 0.0** on 6/24 — the 6/22–6/23 0.4 was old-reagent noise, not real chloramine. No action.
- **⚠️ TA WATCH HAS REVERSED — 2026-08-01 full panel.** TA measured **120 raw** — confirmed: John saw pink at 11 drops, added one more and watched it turn **red at 12**, and Taylor's endpoint is green→red, down from **170 on 6/24**; pH 7.7; CYA 148. CYA-corrected carbonate TA is therefore **~71** (120 − 148/3), versus ~120 back in June. A very wet July diluted alkalinity substantially (rain is essentially alkalinity-free). **The standing trigger below was written for HIGH TA (pH bouncing up); the live risk is now the opposite — LOW carbonate TA means weaker pH buffering, so pH can drift or swing more easily.** ~71 is at the low end but workable for a vinyl pool; still **confirm TA on the next panel before dosing anything**, and **pH 7.7 is fine right now, so NO action yet.** **New trigger: if pH starts drifting below ~7.2, or swings noticeably between tests, raise TA with baking soda** (sodium bicarbonate ≈ 1.5 lb per 10,000 gal per +10 ppm → ~4.7 lb per +10 ppm here; ~9 lb would take carbonate TA ~71 → ~90). Re-measure TA after any further heavy-rain stretch. Caveat: single measurement, and the CYA correction is approximate — confirm the trend on the next panel before dosing anything.
- **🚫 THE POOL HAS NEVER OVERFLOWED — John, 25 years in this house (recorded 2026-08-03).** Overflow would require roughly **23–24 cm** on the skimmer ruler, and John would **drain via the filter** long before that. Season highs have been ~18.5 cm, nowhere near it. **Never hypothesise overflow to explain a water-balance or chemistry discrepancy** — if the level does not match the rain figure, the FEED is wrong, not the pool. (Claude raised overflow on 8/03; it was wrong.)
- **Weather feed: trust it for TIMING, never for AMOUNT (John, 2026-08-03).** It has been reliably right about *whether* rain is coming and roughly *how heavy* the system is — useful for planning (dose earlier, expect a storm-demand day, skip a top-off). It is **never spot-on for what actually lands here**. John's point: rainfall in his town varies widely over just a few miles — North Guilford, the coast and the eastern section can differ dramatically in rain and snow from the same system (cf. the 7/18 storm: Guilford 0.80 in, Northford 3.72 in, Salem 4.68 in). A grid forecast cannot resolve that, and there is no gauge on his property. **So: use the feed to anticipate, use the ruler to measure.**
- **Feed precip errors are large and go BOTH directions — the ruler is sole ground truth.** Documented gaps: 7/18 feed 0.59 in vs pool 1.81 in; 7/22 0.19 vs 0.79; 7/29 0.63 vs 1.69 (all UNDER-statements ~3×); then 8/03 feed **2.89 in vs pool 0.71 in** (a ~4× OVER-statement). Always derive water-in from the level delta (× ~196 gal/cm), never from the precip column.
- **CYA is NOT being lowered by rain alone — mechanism clarified 2026-08-01, and it is worse than that (8/03).** Anchors: 150 (6/24) → 144 (7/12) → **148 (8/1)** — essentially flat despite a very rainy July. Reason: rain *adds* water and temporarily dilutes CYA, but evaporation then removes pure water and re-concentrates it. **CYA mass only leaves the pool when WATER leaves.** And since **overflow never happens here**, the only real routes are **backwash and splash-out** — both small. So passive lowering is effectively negligible; expect CYA to sit ~145 indefinitely unless water is deliberately drained and replaced. (Consistent with the pool having stayed below its true overflow point through July.) Practical read: **CYA is stable ~145**, band floor 10 / aim 15, and no re-tune is warranted on the 144↔148 difference (inside the test's ±15%).
- **TA/pH monitor:** 6/24 read TA 170 / pH 7.6. **CYA-corrected carbonate TA ≈ 120 (normal)** — TA only *looks* high because CYA inflates the reading. No action now. **Trigger to deal with it:** pH repeatedly won't hold below ~7.8–8.0 → then lower TA via muriatic acid + aeration. Flag John at that point.

- **FC demand watch — RESOLVED 2026-06-27 (false alarm):** the 6/26→27 ~3.6 ppm/24h loss looked anomalous only because `weather.py`'s noon snapshot mislabeled a genuinely sunny morning (~5 sunshine hrs, John confirmed "super sunny") as "overcast." It was normal sunny-day loss, not demand. Water clear, no algae, no ladder installed yet. Fixed the tool (sunshine-fraction classifier, v2). **Standing lesson kept: don't skip daily dosing — top to the band each evening;** loss here is variable and skipping let FC dip below floor on 6/27.
- **Water temp:** now tracked daily (79→81°F range late June/early July, trending up with the heat) — record exact readings, one decimal, never round.
- **Water-level tracking (idea, not bought):** John considering a **skimmer water-level gauge** for precise level monitoring. Skimmer is ~5" tall, so buy a standard **cut-to-size adhesive graduated strip** (35mm-wide tape sold by the meter) and trim ~6" — not a pre-made small gauge. With a fixed gauge in the skimmer, a daily photo would let Claude read the level to the fraction-of-an-inch; without one, a bare skimmer photo only gives rough up/down trend (camera-angle noise > the ~3/16"/day changes). Not purchased yet.
- **Daily fills this hot stretch:** John topping off a little most days (well water) to offset evaporation — log each as `EVENT - Fill` with `fill_gal` (minutes × 3.1). Watch the cumulative (Fill total) for the sequestrant trigger and CYA dilution. Cumulative ~251 gal as of 6/30.
- **Loss-rate tuning — DONE 2026-07-01.** Calibrated the evening-dose L24 model from ~2 weeks of data (see "Evening-dose loss model" above; constants in `dose.py` `CONFIG["evening_l24"]`). Supersedes the earlier hand-wave "~2 ppm" guess, which was too low — real L24 runs ~1.5 (rainy) to ~5 (hot & sunny). Predictions now land ±0.3 ppm on clean days. Re-check the fit after a weather regime change (e.g. first cool/cloudy stretch) or if CYA is re-anchored, since loss scales with the FC band.

## Website (GitHub Pages) — planning started 2026-07-01

John wants this project turned into a public dashboard site, hosted on GitHub Pages, so the log data and photos are viewable online anytime.

- **Architecture:** GitHub Pages is static-only (no server, no writes from the live site) — the daily test/dose/log workflow with Claude stays exactly as it is now (unchanged: `daily_calc.py` → judgment → `rows.json` → `append_log.py` → `format_log.py`). A separate build step turns the current state of `Pool_Log.xlsx` + `photos/` into static HTML/JSON, which gets committed and deployed.
- **Content scope (John's call):** "everything" — main landing page is entirely chart-driven (FC/CC trend with the target band shaded, dose history + season total, the new prediction-accuracy chart). Secondary pages/links off the main page: a full raw log table, an `xlsx` download link, the `Taylor_K2006_Testing_Guide.docx` testing guide, and a per-day photo gallery/timeline (each day's chlorine-check photos load on their own page, not crammed onto the main page).
- **Privacy:** explicitly a non-issue — John is fine with pool/yard photos and EXIF data being public ("only be used by me"). No scrubbing needed.
- **Design — DECIDED 2026-07-01:** generated 3 independent visual directions (Workflow fan-out for diversity — instrumentation-panel, sun-warmed logbook, editorial tidewater), presented as real mockups grounded in the actual season data. **John picked "sun-warmed logbook"** — warm coral/teal, DM Sans + Fraunces (serif only on the title/section labels), a big friendly current-FC readout, flat pale-teal target-band shading. Implemented as-is in `site_src/`.
- **Site built and verified 2026-07-01 — this machine is now a git repo** (`git init -b main`, baseline commit, then the site added on a `site/initial-dashboard` branch and merged to `main`). What exists:
  - **`build_site.py`** — reads `Pool_Log.xlsx` + `photos/` + the testing guide, writes `_site/` (gitignored, rebuilt every run, never committed): `data.json`, `index.html` (chemistry trend + target band, dose history, prediction-accuracy chart), `log.html` (full raw table), `photos.html` (day-by-day gallery, photos resized ~6MB→~400KB each — originals stay full-res in `photos/`), plus `Pool_Log.xlsx` and the testing-guide `.docx` copied in for direct download links.
  - **`site_src/`** — the committed HTML/CSS source (`index.html` fetches `data.json` client-side; `style.css` holds the real light/dark palette — this is a standalone site, not a claude.ai widget, so it has its own CSS custom properties, not CDS tokens).
  - **`.github/workflows/deploy.yml`** — on push to `main`, installs `requirements.txt` (openpyxl, Pillow), runs `build_site.py`, deploys `_site/` via `actions/upload-pages-artifact` + `actions/deploy-pages`. Needs the repo's Pages source set to "GitHub Actions" once the repo exists (Settings → Pages).
  - **Verified in a real browser** (not just "should work"): real season data renders correctly in both light and dark mode, prediction-accuracy math checks out exactly (mean error 0.5 ppm, 6/29's +1.7 miss correctly flagged, the pending 7/2 prediction correctly shows no actual bar yet), no console errors, all pages/downloads resolve (200).
  - `.claude/launch.json` has a `pool-site` config (`python -m http.server 8420 --directory _site`) for local preview — run `python build_site.py` then use the preview tool to check changes before pushing.
- **Repo — LIVE 2026-07-01:** [github.com/AirPengwn/pool](https://github.com/AirPengwn/pool). Remote wired up (`git remote add origin`), the placeholder GitHub-generated README merged in (`--allow-unrelated-histories`, kept our real README over the one-liner), pushed to `main`.
- **Pages-source gotcha (found + fixed 2026-07-02):** the repo's Pages source defaulted to **"Deploy from a branch"** (GitHub's legacy Jekyll pipeline, auto-triggered on every push, keyed off `README.md` at branch root since there's no root `index.html`). This runs **in addition to, and races against**, our `deploy.yml` Actions workflow — both fire on the same push, and whichever finishes last wins the live URL. Result: the site showed stale README content (Jekyll's auto-render) instead of the real dashboard, even though `Deploy pool dashboard` itself succeeded every time. **Fix:** Settings → Pages → Build and deployment → Source → **GitHub Actions** (not "Deploy from a branch"). Confirmed via the GitHub API (`/repos/.../actions/runs`) that a `pages build and deployment` run was firing alongside ours at identical timestamps — that auto-workflow only exists in branch-deploy mode, so its presence is the tell if this regresses. **If the live site ever reverts to showing README content again, check this setting first.**
- No `gh` CLI on this machine, and no GitHub API auth token — verification of live-site/Actions/Pages state goes through `WebFetch` (public, unauthenticated) against `github.com/.../actions`, `api.github.com/repos/.../actions/runs`, and the live Pages URL directly. Settings-page changes (like the Pages source toggle) need John — no way to flip repo settings remotely from here. **Gotcha: `WebFetch` caches responses per-URL for ~15 min** — after a fix/redeploy, re-check the live site with a cache-busting query string (`?cachebust=...`), or better, drive a real browser (`claude-in-chrome`) since `WebFetch` doesn't execute JS anyway and can't see chart data or JS-rendered hero stats, only static HTML.
- **Second caching gotcha (2026-07-02):** the real browser has its own HTTP cache too — re-visiting the *same* production URL right after a redeploy, in a tab that already loaded it earlier in the session, served a stale `index.html`/JS bundle (chart rendered with old/wrong-looking data even though `data.json` itself was fresh — confirmed by fetching `data.json` directly). Fix: append a throwaway query string (`?fresh=<n>`) when re-checking a URL already visited this session, forcing a real fetch instead of a cache hit. Don't mistake this for a real bug — check data.json fresh first before concluding the deploy broke something.
- **LIVE AND VERIFIED 2026-07-02:** confirmed via a real browser (not just `WebFetch`) at `https://airpengwn.github.io/pool/` — real dashboard renders correctly (FC 14.8 in target band, CC 0.2, water 81.0°F, 18.0 gal season dose total, full 3-chart layout including the pending 7/2 prediction). Site is done and working end to end.
- **Real (not just testing) caching fixes (2026-07-02):** these aren't just verification annoyances — they affect actual site visitors:
  - **`data.json`** — a visitor whose browser cached it on an earlier visit could keep seeing stale numbers on later visits, defeating the point of a *daily*-updating dashboard. Fixed: `fetch('data.json', { cache: 'no-store' })` forces a real network request every load.
  - **`style.css`** — same problem one layer up: after a design change, a visitor's browser could serve fresh HTML (new CSS classes) paired with an old cached stylesheet lacking those rules — caught this exact way when a photo thumbnail rendered at full native size (1600×1200) instead of the intended 92×92 because the cached CSS predated the `.hero-thumb` rule. Fixed: `build_site.py` now version-busts the stylesheet link on every build (`style.css?v=<build timestamp>`, computed once in `main()` and threaded through `index.html`/`log.html`/`photos.html`), so a redeploy always pulls a matching fresh stylesheet.
  - General lesson: any *asset the HTML depends on* (not just the HTML page itself) needs its own freshness story on a site that changes often — don't assume cache-busting the outer page is enough.
- **Sideways photos on the site — general fix, not a one-off (2026-07-02):** John flagged 3 specific dates (7/1, 6/30, 6/28) as needing manual 90° rotation. Investigated before touching anything: the *original* photos in `photos/` are actually fine — they carry a standard EXIF orientation tag (phone sensor captures landscape, tag says "rotate N to display upright"), which most viewers including browsers respect automatically. The real bug was in `resize_photo()` (`build_site.py`): opening + resizing + resaving via Pillow silently drops EXIF metadata, so the *web copy* loses the rotation instruction and displays sideways even though the source file is correct. Fixed at the source with `ImageOps.exif_transpose(im)` right after opening, before resize/convert. This is a general fix, not per-date — scanning all photos found the same bug also silently affecting `2026-06-29_1.jpg` (orientation 6) and `2026-06-30_3.jpg` (orientation 3, a 180° case), neither of which John had noticed/flagged. **Do not hand-rotate individual original photo files** — if a photo ever looks sideways on the site again, check `im.getexif().get(274)` on the original first; the fix belongs in `resize_photo()`, not in `photos/`.
- **Light/dark theme toggle added (2026-07-02):** explicit UI control in the header (button, top-right of nav) in addition to the existing automatic `prefers-color-scheme` behavior. Preference persists via `localStorage` (`theme` key: `"light"` or `"dark"`), applied via `data-theme` attribute on `<html>`. `site_src/theme.js` (copied + version-busted like style.css) runs synchronously in `<head>` before first paint to avoid a flash of the wrong theme, and exposes `window.getTheme()` (checks the explicit attribute first, falls back to `matchMedia`) — used by both the toggle button and the chart JS's `gridColor` logic, so charts respect a manual override too. Toggling reloads the page rather than live-patching styles/charts, trading a little snappiness for full consistency (avoids partial-update bugs across already-rendered Chart.js canvases). CSS: dark values now live in two places by necessity (the `@media (prefers-color-scheme: dark)` block, scoped with `:not([data-theme="light"])` so an explicit light choice can override system dark preference, **and** a `:root[data-theme="dark"]` block) — if dark-mode colors are ever retuned, update both.
- **Light-mode background — iterated twice (2026-07-02):** first pass (contrast fix) deepened `--bg` to a warm tan (`#EFE6D8`, was `#FAF7F1`) and added `--card-shadow` — this fixed the real problem (cards weren't visually separating from the page) but John still didn't like the *color itself* on a second look ("still doesn't seem great"). Rather than guess a third time, tried two genuinely different hue directions (not just tan variants) and screenshotted both against the real dashboard: a cool pool-water blue-gray (`#DDE6E8`) and a neutral warm greige (`#E4E1DA`). Shipped the blue-gray — it ties thematically to the pool/water content and works with the existing teal accent — but the greige is a ready alternative if this one doesn't land either. **If backgrounds get revisited again, try a genuinely different hue, not another shade of the same one that already got rejected.** `--card-shadow` and the stronger `--border` from the first pass are unrelated fixes and stayed.
- **Table colors reverted to a stronger blue (2026-07-02):** round 6 had made the header + totals row a pale blue wash (`#DCEAEF`) to "stand out." John didn't like it — wanted "blue again" (closer to the Excel sheet's own header, `FF1F6FB2`) and wanted the data rows themselves kept strictly neutral (white/gray, no warm tint). Added `--table-header-bg: #1F6FB2` (same strong blue in both themes, since it's a solid saturated fill rather than a subtle tint) with white text on the header + totals rows; `--row-shade` (alternating day bands) changed from a warm-brown tint to plain neutral gray. `--totals-bg` is gone (no longer used by anything).
- **Testing quirk: small/edge-positioned elements can get wrong click coordinates from the `find`+`ref` click path (2026-07-02):** the new 32×32px theme-toggle icon button, positioned near the far edge of an ultra-wide (3440px) real viewport, silently failed to register clicks via `ref`-based clicking even though `find` correctly located it — repeated clicks produced no state change at all. Diagnosed by (1) dispatching a synthetic click event directly in JS (worked perfectly, proving the button's own JS logic was correct) and (2) reading `getBoundingClientRect()` and clicking those exact real pixel coordinates directly (also worked). This was purely a coordinate-mapping issue in that specific click path for this specific case, not a site bug — but it's a real testing gotcha worth remembering: **if a `ref`-based click on a small element silently does nothing, don't assume the site is broken — try a synthetic `dispatchEvent` first to isolate JS-vs-click-tool, then fall back to a coordinate-based click using the element's real `getBoundingClientRect()`.**
- **LIVE AND VERIFIED 2026-07-02:** confirmed via a real browser (not just `WebFetch`) at `https://airpengwn.github.io/pool/` — real dashboard renders correctly (FC 14.8 in target band, CC 0.2, water 81.0°F, 18.0 gal season dose total, full 3-chart layout including the pending 7/2 prediction). Site is done and working end to end.
- **Chemistry chart "safe & swimmable" band (added 2026-07-02):** a second, wider, lighter shaded band from `fc_floor` up to a hardcoded `SAFE_SWIM_CEILING = 19` in `site_src/index.html`'s chart JS — grounded in POOL.md's own note that FC "up into the high teens" is safe/swimmable at this pool's CYA (~150). **Not CYA-scaled** (no formula given for this ceiling, unlike the target band's `fc_floor`/`fc_aim` which do scale with CYA via `dose.py`'s `band_for_cya`) — if CYA is meaningfully re-anchored, revisit whether 19 still makes sense. **Legend added 2026-07-02** (John asked "why two blues?"): an inline color-chip key under the chart labels FC, CC, the darker "Daily target (floor–aim)" band, and the lighter "Safe to swim (up to ~ceiling)" band (chip has a dashed top echoing the ceiling line). Ranges pulled from `meta`/`SAFE_SWIM_CEILING` so they stay correct if CYA is re-anchored. Caption trimmed to just "Free and combined chlorine, season to date" since the legend now carries the band explanation.
- **Round 9 — big chart expansion (2026-07-02):** John supplied a 14-item wishlist of new dashboard visualizations plus 6 "clever idea" concepts, framed explicitly as "add them all, decide on show/hide/collapse later — I want to see them all first." Skipped only the 2 that duplicated existing charts per his own instruction (Free Chlorine Timeline ≈ existing Chemistry chart; Prediction Accuracy line ≈ existing bar chart). Built everything else and added to `index.html` in one pass:
  - **Data pipeline:** `build_data_json()` now exposes `fc_loss`, `ph`, `ta`, `cya`, `rain_in` per test row and a new top-level `fills` array (date + gallons per `EVENT - Fill` row) — previously only `fc`/`cc`/`water_temp`/`sun_hrs`/`pred_error` were threaded through.
  - **New charts/cards:** FC gauge (half-doughnut, zones scaled to `fc_floor`/`SAFE_SWIM_CEILING`, needle via a thin overlaid wedge dataset), daily FC-loss bar chart (green/amber/red by severity), a GitHub-style chlorine-loss calendar heatmap (hand-built CSS grid, not a Chart.js type), 4 scatter charts (weather/temp/rain/dose-added vs. FC loss), pH/TA/CYA trend lines, cumulative chlorine used, weekly chlorine usage, cumulative fill water, a chlorine-efficiency stat card, a first-draft weekly "stability score" (0–100, explicitly labeled as an invented rubric to be tuned later, not a standard metric), a season-so-far stat grid, and a chemical-cost calculator that takes a **client-side price input** (localStorage only, nothing fabricated or sent anywhere) rather than guessing a $/gal figure.
  - **Explicitly skipped, not forgotten:** the "Forecast Graph" idea (multi-day forward FC projection) needs a real multi-day weather forecast fetch — `weather.py` currently only pulls `forecast_days=1` (today) — plus chaining the just-calibrated single-day L24 model forward, which is materially more architecture than a chart. Open-Meteo (already in use) supports multi-day forecasts trivially; flagged to John as a good next-round candidate rather than rushed/faked here.
  - **Real bug caught during verification, not just a rendering fluke:** the 2 pre-6/24 CYA readings are stored as approximate strings (`"~100"`, `">100"`, old-reagent era) rather than numbers. A naive `!= null` filter let them through and Chart.js silently dropped the non-numeric points from the CYA line chart (only the one real numeric 150 reading showed). Fixed by filtering on `typeof t.cya === 'number'`, with the caption now saying explicitly how many approximate old-reagent readings were excluded — matches POOL.md's own "pre-6/24 data excluded, unreliable" stance elsewhere.
  - Page is now quite long (3 original + ~18 new cards). **Toggle/show-hide/collapse UI is an intentionally deferred follow-up** — John wants to review what's actually useful before any of that gets built.
- **Round 10 — organize the round-9 sprawl (2026-07-02):** John liked round 9 ("lots of data and cool stuff") and doesn't want to cut anything yet, but asked for a way to navigate the now-long page: "organize this... make them possibly expandable... a table of contents or menu near the top to jump to." Also set the chemical-cost default to his real current price, $5.99/gal (was empty).
  - **Grouped the ~19 chart cards into 4 collapsible sections** using native `<details>/<summary>` (no JS framework needed): *Chemistry trends* (Chemistry, daily FC loss, loss calendar, pH, TA, CYA), *Correlations* (the 4 scatter charts), *Chlorine usage & cost* (Doses, cumulative/weekly usage, fill water, efficiency, cost calculator), *Predictions & season recap* (prediction accuracy, stability score, season summary). Hero card + FC gauge stay ungrouped at the very top — always-visible "glance" info, not buried behind a toggle.
  - **All sections default `open`** — nothing is hidden on first visit, matching "don't want to get rid of anything yet." Collapse state persists per-section via `localStorage` (`section-<id>`), read synchronously before the section renders so there's no flash-of-wrong-state.
  - **Sticky jump nav** added below the masthead (`.section-nav`, `position: sticky; top:0`) linking to each section's anchor id. `scroll-margin-top` on `details.section` keeps the target header from landing underneath the sticky bar; `html { scroll-behavior: smooth }` for a non-jarring jump.
  - Chevron (▸/▾ via a rotating `::before` on `summary`) shows section state at a glance; section header styled to match the site's existing serif card-title convention with a teal underline.
- **Push routine — standing authorization (confirmed 2026-07-01; cadence updated 2026-07-03):** pushing is part of the normal routine, same as running `format_log.py` — **no need to ask before pushing.** **Push on EVERY report-in, i.e. twice a day** (once after the noon TEST, again after the evening DOSE), not bundled once at day's end (John's explicit request 2026-07-03 — he wants the live site current each time he reports numbers). Each time: after `append_log.py` + `format_log.py`, run `python build_site.py`, `git add` the changed `Pool_Log.xlsx` (and any new `photos/`) + rebuilt source, commit, `git push` to `main` (the deploy workflow builds `_site` and pushes to `gh-pages`; Pages serves from there). Report that it's done; don't ask first. (Durable authorization per John's explicit request, not a one-off approval.) Note: `build_site.py` reads the workbook directly, so a commit of just `Pool_Log.xlsx` is enough to refresh the live charts — no separate rebuild of `_site/` locally is needed before pushing (the Action rebuilds), but rebuilding locally is fine for a pre-push preview.
- **Deploy mechanism switched away from `actions/deploy-pages` (2026-07-02):** round 10's deploy got stuck — the Pages Deployments API (`actions/deploy-pages@v4`) started failing intermittently: deployments were created fine and reached `in_progress` within seconds every time, but then hung anywhere from 16 seconds to the full 10-minute Action timeout before erroring out, with no consistent pattern (3 of 4 consecutive attempts failed; one succeeded cleanly in ~4 min). Confirmed via `gh run watch`/`gh api repos/.../deployments/{id}/statuses` (once `gh` was installed + authenticated — see below) that this was GitHub's own Pages backend, not anything in our build (build + artifact upload succeeded every single time) or a stuck orphaned deployment blocking the queue (ruled out by checking deployment history directly). Toggling the repo's Pages source setting off/on did **not** fix it.
  - **Fix — John's idea:** stop using the Pages Deployments API entirely. `.github/workflows/deploy.yml` now builds `_site/` exactly as before, then does a **plain `git push`** (no third-party action) of its contents to a dedicated `gh-pages` branch — clones the branch if it exists (or `git init -b gh-pages` on the very first run), replaces its contents, commits on top of prior history, pushes. One commit per deploy, so **every past version of the deployed site stays in `gh-pages`'s own git log** — this was an explicit ask (John initially proposed pointing Pages at `main` directly for this, but `main` holds only source, not the built site — the real `index.html`/`data.json`/resized photos only ever exist in the gitignored `_site/` output; serving `main` as-is would resurrect the exact stale-README/Jekyll bug from earlier today. `gh-pages` gets the same two things John wanted — the Pages source setting only needs to be set once, and full version history is preserved automatically — without that conflict).
  - **`permissions: contents: write`** replaces the old `pages: write` / `id-token: write` (no longer needed — plain git push over HTTPS using the built-in `GITHUB_TOKEN`, no OIDC/Pages-API round-trip). `.nojekyll` is written into every deploy so GitHub never tries Jekyll-processing the site.
  - **One-time manual step needed from John:** Settings → Pages → **"Deploy from a branch"** → branch `gh-pages` / folder `/ (root)`. Not something Claude can do (Settings-page changes need repo admin access this session doesn't have).
  - **`gh` CLI installed + authenticated this session** (`winget install --id GitHub.cli`, then John ran `gh auth login` himself in his own terminal — device-code browser flow, no token ever pasted into chat). This is what made the actual diagnosis possible (`gh run watch`, `gh api repos/.../deployments/{id}/statuses` to see real state transitions) — before this, verification was limited to unauthenticated `curl` against the public API, which 403's on log downloads. Note for future sessions: `gh` resolves fine in new terminal windows/sessions (winget registered it in the machine PATH correctly), but any *already-running* shell process from before the install won't see it without an explicit PATH refresh or restart.
  - **Root cause confirmed 2026-07-02 ~17:00 — a real GitHub-wide incident**, not anything on our end: [githubstatus.com](https://www.githubstatus.com) posted "Incident with Pages" (investigating degraded/failing Pages deployments) starting 16:54 UTC, right when our failures ramped up. Explains everything: why it worked fine earlier that morning (3 clean successes 01:56-03:30 UTC) then broke consistently from ~15:17 onward; why toggling the Pages source setting didn't help; why switching to a `gh-pages` branch didn't help either (GitHub's own auto-generated `pages-build-deployment` workflow still routes through the same broken Deployments API backend regardless of source mode — confirmed both modes hit the identical failure signature). **Lesson for next time this happens: check githubstatus.com before spending hours on repo-side troubleshooting** — would have saved most of this afternoon. Drafted (but didn't need to file) a GitHub Support ticket with the full timeline/deployment-ID evidence; kept the git-push-to-gh-pages workflow anyway since it's simpler and doesn't depend on the OIDC/Deployments-API round-trip at all, so it should be less exposed to this class of incident in the future — but the ultimate publish-to-CDN step for a branch-sourced Pages site apparently still isn't fully independent of it.
- **Concert page (added 2026-07-09):** John wanted a standalone, shareable gallery of concert photos+video, reachable from a **small "concert" link at the very bottom of the dashboard** (`site_src/index.html` → `.site-footer`, muted 12px). Generates `concert.html` — its own page (masthead + theme toggle, no pool data): a *Video* section (grid of `<video controls preload="metadata">` players) above a *Photos* section (thumbnail grid → same lightbox as `photos.html`). Purely additive; touches none of the daily-log or chart code.
  - **Media pipeline mirrors the pool-photo one.** Raw iPhone originals get dumped into `icloud/` (**gitignored** — ~100MB of camera files, local-only). Web-ready re-encoded copies live in a **committed `concert/`** dir; `build_site.py`'s `scan_concert()` just *copies* `concert/*` → `_site/concert/` and `build_concert_html()` writes the page. **The CI runner has no ffmpeg**, so (exactly like the pool `.mp4`s) all transcoding is done once offline and only the small web-ready files are committed.
  - **Transcode recipe (one-off, `prep_concert.py` in scratchpad):** photos → `ImageOps.exif_transpose` + resize to 1600px, q82 (~130–490 KB each). Videos → **the originals were already H.264 720p** (~3.4 Mbps, so a 66s clip = 30MB) — not HEVC, so browser-compatible already, but re-encoded anyway for two reasons: (1) **+faststart** (moov atom to the front, so playback starts before full download — iPhone MP4s lack this), (2) size. `ffmpeg -i IN.MP4 -c:v libx264 -crf 26 -preset medium -movflags +faststart -c:a aac -b:a 128k -pix_fmt yuv420p OUT.mp4` — CRF 26 roughly halved them (CRF 23 barely shrank them since the source was already efficient); 6 videos + 19 photos = **~57MB committed**. ffmpeg auto-rotates portrait clips. **To add/refresh concert media:** drop originals in `icloud/`, run that recipe into `concert/`, then `build_site.py` picks them up.
  - **Verified in a real browser (2026-07-09):** page renders in the pool theme, a video actually *plays* (decoded 1280×720, `currentTime` advanced, no media error — the `ERR_ABORTED` on the `.mp4` network rows is expected for `preload="metadata"`: browser reads the faststart header, then cancels the rest until play), all 25 media load 200, footer link resolves to `concert.html`, no console errors.
- **Concert page -> two-night structure (2026-07-12):** John saw a 2nd Noah Kahan show, so the feature is now two standalone gallery pages -- `concert.html` (Night 1, July 8) and `concert2.html` (Night 2, July 11), both Fenway Park, Boston. A `.night-nav` pill toggle (Night 1 / Night 2) sits right below the masthead on each. `build_site.py` now drives it from a `CONCERTS` config (per night: page, media dir, `web` path, heading incl. venue, `<title>`, `setlist`, `encore`) via generalized `scan_concert_dir()` / `build_concert_page()` / `setlist_html()`. Each page renders the **setlist** (main + encore, continuous numbering, 2-column) ABOVE the media. Night 2 media = 27 photos + 16 videos: raw camera originals in `icloud/night2/` (gitignored), web-ready in the committed `concert2/` (~128MB), processed by `prep_concert2.py` (same recipe: photos 1600px q82; videos H.264 720p CRF26 +faststart -- one portrait clip auto-rotated fine). Home-page footer link renamed `concert` -> `concerts`. **Setlists sourced from setlist.fm (fan-submitted -- flagged to John to verify.)** Verified in a real browser: both pages render, night-nav switches + active state correct, 25-song setlists, a Night 2 video plays (no errors). To add a future night: drop raw in a gitignored stash, run a prep script into a committed `concertN/`, add a `CONCERTS` entry.
- **Not yet done:** a nicer inline-rendered testing-guide page (V1 just links the raw `.docx`), per-day photo pages (V1 is one `photos.html` with day-anchored sections rather than separate pages per day — fine for now, revisit if it gets long). Worth a future glance: `photos/` originals are ~6MB each and committed as source — at a photo-heavy pace that adds up over a season (still well within GitHub's limits, just a "watch this" not a blocker).

## Changelog (append notable standing-rule changes here)

- 2026-06-22: Project initialized for Claude Code. Migrated from chat-based upload/download workflow.
- 2026-06-23: Noted environment (Windows — use `python`/`C:\Python314`, not `python3`). New FAS-DPD reagents expected Thu 6/25; CYA recheck held until then.
- 2026-06-23: Added `weather.py` (Open-Meteo, no API key) — weather now pulled automatically at noon; user no longer needs to report sky conditions.
- 2026-06-23: Fresh FAS-DPD reagents arrived early. Full battery + CYA recheck moved up to 2026-06-24 noon.
- 2026-06-23: Pump put on an automatic 7 AM–10 PM timer. Stop logging daily PUMP rows; assume runtime, log only deviations.
- 2026-06-25: Switched dosing to the EVENING (~8–8:30 PM, by 9:15 latest). Test stays at noon. Cuts UV loss, better overnight FC, more predictable projections. Project next-noon FC after an evening dose using ~2 ppm loss, not the daytime bucket.
- 2026-06-27: `weather.py` v2 — bucket now keys off realized sunshine-hours fraction (hourly), not the instantaneous cloud snapshot, which had mislabeled a sunny morning as overcast. Log string now includes "sun so far Xh of Yh daylight."
- 2026-06-27: Added a **Sun (hrs)** column to the Log — actual daily sunshine hours, recorded on each TEST row. Backfilled 6/19–6/27. `append_log.py` updated (sun_hrs key/arg); `weather.py --history N` fetches daily totals.
- 2026-06-27: Log presentation cleanup — removed header auto-filter; alternating-day gray shading (`C4C4C4`) with thick per-day box borders + thin inner grid; centered all cells except Weather/Notes (left+wrap); auto row heights. New script `format_log.py` applies all of this idempotently — **run it after every `append_log.py`.**
- 2026-06-27: Expanded Log to 18 columns — added **FC loss (ppm/24h)**, **Cum. Cl (gal)**, **Rain (in)**, **Water temp (°F)**, **Photo**. Backfilled FC loss / cumulative chlorine / rain / sun for 6/18–6/27. `append_log.py` + `format_log.py` updated for the new layout.
- 2026-06-27: Started **daily chlorine-check photos** (`photos\YYYY-MM-DD.jpg`) — Claude analyzes each for clarity, early algae, and stain progression.
- 2026-06-30: Permissions — added `Bash(python:*)` allow-rule in `.claude/settings.json` (no prompts for pool scripts). Scripts made path-independent; photo renaming moved to `file_photos.py` (consistent command). Font unified to Calibri 11 across the sheet; Water temp column now shows one decimal (0.0) — never round reported values.
- 2026-06-30: Added **Fill (gal)** column and a **SEASON TOTALS row (row 2)** with live SUM formulas over Chlorine/Rain/Fill; data now starts row 3; top 2 rows frozen. `append_log.py` style template made dynamic (last row) instead of a hardcoded row number.
- 2026-07-01: Resolved the recurring permission-prompt issue — enabled the app's **"Allow bypass permissions mode"** toggle (the real fix; `.claude/settings.json`/`settings.local.json` edits did not reliably work). Added **`daily_calc.py`** to consolidate the noon weather+loss+dose calc into one script driven by `today_input.json`.
- 2026-06-27: Captured John's manual column widths into `format_log.py` WIDTHS (re-applied every run; re-capture if he readjusts). Added min row height 20 for single-line rows; multi-line rows stay auto. Relabeled the two EVENT rows (`EVENT - Open`, `EVENT - Storm`).
- 2026-07-01: Built the GitHub Pages dashboard — `git init`, `build_site.py` (xlsx/photos → static site), `site_src/` (sun-warmed-logbook design, John's pick from 3 mockups), `.github/workflows/deploy.yml`. Verified locally in a browser (real data, both color schemes, correct prediction-accuracy math, no console errors). Repo/first push still pending John's repo URL. See "Website (GitHub Pages)" above for the full state and the daily push routine once live.
- 2026-07-01: Fixed a `format_log.py` fill bug — fill colors were 6-digit hex, which openpyxl stores with a transparent `00` alpha, so the SEASON TOTALS row **and** the alternating-day gray shading were rendering white/invisible. All fills now 8-digit ARGB (opaque `FF…`). Row 2 recolored to a more visible blue (`FF9DC3E6`, up from the pale `DDEBF7`) so it stands out under the header; header blue (`FF1F6FB2`) is now hardcoded (was copied from A1, which could silently degrade to white with white text). Gray day-shading (`FFC4C4C4`) now actually shows. **Rule: any fill color in the format scripts must be 8-digit ARGB.**
- 2026-07-01: Started **forward prediction tracking** (John's request) — each evening logs a next-noon FC prediction on the DOSE row; each noon scores it on the TEST row. Initially inline in Notes text, then **promoted to two dedicated columns same day** (John's follow-up request): **Pred next-noon FC** (col 8) and **Pred error (ppm)** (col 9), inserted right after FC loss — Log is now 21 columns (was 19). Backfilled the in-sample baseline (6/27→28…6/30→01, clean-pair err 0.0/+1.7-miss/+0.4/0.0) into the new columns and trimmed the now-redundant PRED text out of Notes. `append_log.py`/`format_log.py` updated for the new column map (`pred_fc`/`pred_error` keys; error column uses a signed `+0.0;-0.0;0.0` number format). Season-totals SUM formulas (now M2/P2/Q2) fixed after the column insert. Backup taken before the restructure: `Pool_Log.backup-2026-07-01-precolinsert.xlsx`. First true out-of-sample test: 7/1→7/2, predicted 15.1 (row 42). See "Prediction tracking" above.
- 2026-07-01: Closed the prediction-tracking manual-math gap — `daily_calc.py` now auto-finds last night's DOSE-row prediction and prints the noon `Pred error` already computed (no more manual subtraction). `dose.py` gained a `--evening --dose X` mode so the evening projection can be recomputed for whatever dose actually gets poured (may differ from the model's own default — confirmed on 6/27, where the model's own rec was 2.0 gal but 1.5 was actually dosed). Both tested against real historical/current data (no fabricated readings) before landing.
- 2026-07-17: **Pool volume corrected 34,400 → ~31,400 gal (FULL), and the reagent saga CLOSED — one root cause.** John measured the geometry (40×20; 12 ft shallow shelf @ 3 ft; deep hopper 10×14 flat @ 8.5 ft; **all perimeter walls 3 ft** — new `pool_volume.py` models it as a 3-ft slab + hopper frustum). The bowl-shaped hopper holds ~9% less than the old avg-depth-5.75 estimate. That overestimate is what made the FAS readings look "impossible" and inflated the over-read to ~1.5×; the old reagent actually over-read only **~1.2×**. With the fresh **TF-100** kit John measured **FC 17.6 (88 drops), reproduced on two proper first-clear titrations** → real FC logging resumes (7/11–7/16 blank-estimate era closed). `dose.py`: `pool_gallons` 34400→31400, `ppm_per_gallon` 3.5→3.84. **Watch:** (a) the smoke week ran ~20% higher than the "corrected" estimates (7/16 true ~18, not ~14.5) — always safe; (b) `evening_l24` was fit to the old 3.5, so re-validate forward pred errors and expect to nudge `l24_*` back UP. **New level→volume method:** top 3 ft has vertical walls (surface const. ≈ 800 sq ft) → **~196 gal/cm** on the skimmer ruler; daily Δcm × 196 = gallon loss/gain; absolute anchor = shallow depth (36 in full; 34.5 in ≈ 30,700 gal current). Deploy Actions incident (since 7/9) also cleared — auto-deploy via `git push origin main` works again; manual gh-pages workaround dropped.
- 2026-07-17 (back-test — `backtest_volume.py`): Validated the volume correction against the season's own prediction record (12 good-reagent dose→next-noon pairs). **Two independent lines converge:** the dose response the chlorine data alone "wanted" (`implied_R = 3.5 + error/gal`) averages **~3.85 ppm/gal** (median 4.1; clean-pairs mean ~4.0) — matching the **3.84** from measured geometry (a fit on the chlorine data, totally independent of the pool's dimensions). Re-running at 3.84 collapses the **mean signed prediction error +0.42 → +0.04 ppm** (near-unbiased): the season-long "actual always ran a bit high" was mostly the ~9% volume overestimate (doses landing harder than modeled). MAE barely moves (1.14→1.03) — the rest is real weather/demand scatter and one-off events (7/9 dog −3.0, 7/5 party −1.3), which correctly get slightly *worse* under 3.84 (implied_R ~0.5 — the tell they were excess-demand, not weak-dose). Caveat: the bias had two same-direction causes — weak R (volume) **and** the 7/4 hot-day loss over-estimate (already fixed by lowering l24) — partly substitutable, so not *all* of it is volume; but 3.84 cleanly removes the residual that survived the l24 fix. **⚠️ This CORRECTS the earlier "nudge `l24_*` back up" guidance:** the back-test shows 3.84 + the **current** l24 is already near-unbiased (residual +0.04 all / +0.25 clean — still a hair *under*-predicting), so **do NOT raise l24.** Leave it, watch forward errors; if the small positive residual persists, it points to R ~3.9 (the pool often sits below full → current ~30,700 gal) rather than an l24 change.
- 2026-07-19: **Sample-location settled, and a big-demand day that RETRACTS the loss-model lean.** (a) John asked whether where he draws the sample matters — paired samples (deep end by the ladder 58 drops vs mid-pool 57) came out one drop apart, inside the test's resolution → pool is well mixed, location doesn't bias readings (see Testing method). (b) FC fell 15.8 → 11.6, and the skimmer ruler let us split it **exactly**: rain added ~902 gal (30,594 → ~31,496), so dilution was only **0.45 ppm** and real chemical demand was **3.75 ppm/24h**. With the prior days that gives dilution-corrected losses of **1.7 / 0.2 / 1.8 / 3.75** — loss is **genuinely noisy, not systematically over-modelled**, so **the earlier lean toward halving `l24_sunny` is retracted**; the original "~3 ppm background ± large scatter" finding is holding up. Loss also ran *backwards* vs sun (the 3.5 h-sun rain day lost 3.75; the 12 h-sun day lost 1.8), consistent with organic load (rain washing debris in + smoke deposition) driving demand rather than UV. **Lesson: do not retune `l24_*` off a handful of points.** (c) **Rain-gauge finding:** the weather feed logged 0.59 in for 7/18 but the pool gained ~902 gal ≈ **1.81 in equivalent (~3×)** — a localised downpour the grid missed and/or deck runoff draining in. **The ruler is ground truth for the pool's water balance; the precip column under-reports what actually arrives — use the level delta (× ~196 gal/cm) for dilution math.** 7/18's Rain cell corrected 0.89 (mid-day forecast) → 0.59 (measured actual). Pool now essentially full (16.3 cm ≈ 31,500 gal), so further rain overflows — which usefully dilutes CYA (~144 → ~140; **recheck due**, last anchor 7/12).
- 2026-07-19: **Full loss-model re-analysis on the corrected volume — `analyze_loss.py`.** Rebuilt the daily-loss dataset tracking chlorine as **mass** (`volume × FC`), with each day's true volume read off the skimmer ruler, so rain dilution and level changes are handled *exactly* instead of being smeared into "loss". 9 clean 1-day pairs, all with exact levels. **(1) Mean loss = 3.54 ppm/24h** (stdev 1.46, range 1.8–6.9) → the existing `l24_*` buckets (2.5–3.5) were **right all along**; the earlier "runs 2× hot" lean was simply wrong — it came from raw FC differences on no-dose days rather than a mass balance. **(2) Nothing forecastable predicts the variance:** water temp r = **−0.00**, sun hours r = +0.49 (n=6, not significant), and an apparent FC relationship (r = −0.73, nominally significant) **collapsed under a robustness check** — dropping the one dog-load day → r = −0.47 (n.s.), dropping all documented special-cause days → r = **+0.11**. One outlier, not physics; the "first-order" fit that won on MAE was pure overfitting. **(3) The sun-bucket structure earns nothing:** flat-mean MAE 0.97 vs buckets 0.98 — identical. **(4) The real structure is episodic, not meteorological:** the 5 quiet days sit at **3.07 ± 0.61 ppm** (very predictable), while the 4 days with *documented* events swing hard — dog/bather load 6.9, July-5 party 4.0, post-storm organics 3.8, heavy smoke 1.8. (Days were flagged from pre-existing log notes, not chosen to fit the curve.) **Practical ceiling: ~±0.6 ppm on quiet days, ~1.0 MAE overall** — no weather variable improves on that. To predict better we'd have to log the *actual* driver (bather/dog load, storm) rather than the sky. **Caveats:** n=9; only 2 pairs from the TF-100 era; the mass constant assumes full-strength 12.5% stock, so degraded chlorine would inflate computed loss on dosed days.
- 2026-07-19 **(applied, John's call):** **`l24_*` flattened to 3.5 across all four buckets** — matches the measured 3.54 mean, removes the +0.49 under-prediction bias, and drops weather structure the analysis showed was doing nothing (flat-mean MAE 0.97 vs buckets 0.98). The `rain_sun_max` / `partly_sun_max` / `hot_temp` thresholds stay in CONFIG so the buckets can be re-differentiated later, but are **currently inert**. **New `Load` column (col 23)** added to the Log + `append_log.py` / `format_log.py` / `build_site.py` / `analyze_loss.py` — see the column docs above for the convention and vocabulary. Backfilled from documented notes (7/06 party, 7/10 dog, 7/17–7/18 smoke, 7/19 storm); **the `quiet` entries for 7/04–7/09 are INFERRED** from the absence of any recorded event in an otherwise detailed log, *not* positively logged at the time — treat them as weaker evidence. **Next step: once several weeks of load flags accrue, re-fit L24 per load class** and replace the flat 3.5 with per-class values.
